import React, {Component} from 'react'
import { connect } from 'react-redux'

class Totals extends Component {
  render(){
    return(
      <div>
        Totals
      </div>
    )
  }
}

export default connect(null)(Totals)
